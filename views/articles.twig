<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<ul id="navigation">
    {% for article in articles %}
        <div>
            <b>Title:</b> {{ article.getTitle() }}<br>
            <b>Excerpt:</b> {{ article.getExcerpt() }}<br><br>
            <b>Images:</b><br>
            {% for image in article.getImages() %}
                (Name: {{ image.getName() }} |
                Url: {{ image.getUrl() }} |
                Size: {{ image.getWidth() }} X {{ image.getHeight() }})
            {% endfor %}
        </div>
        <hr>
    {% endfor %}
</ul>
</body>
</html>